# 更新日志

## v0.5.2

- 新增:GIF 自建预览管线——WIC 自解码 + 原生内存帧缓存(引用计数,帧归零即释放)+ 虚拟化联动播放,替代 BitmapImage 直播(高 CPU/内存不可控)
- 新增:解析缓存(默认开,LRU 预算 1GB 全库帧驻留,滚动回看零解码);会话上限 48(最大化视口全部卡片有动画)
- 新增:软挂起——闲置 3 分钟停播全部 GIF 并清缓存,任意输入唤醒
- 新增:壁纸预览模糊、日志级别切换、订阅筛选
- 变更:GIF 调优路径简化——移除解码并发闸门/错位启动/滚动预热/200ms 停留门槛;节流 GC 改非阻塞;备货 1 屏(CacheLength=1)减少容器实化 churn
- 变更:Win2D 兼容性修复与 UI 细节
- 移除:调试用诊断代码(启动 GC 采样、内存构成输出、播放器诊断计数)

## v0.5.1

- 新增:壁纸/组件列表全面 GridView 化——筛选/排序时被剔除的项消失、剩余项平滑补位移动(100ms Composition 隐式动画,CommunityToolkit.WinUI.Animations),新增项即时插入;窗口缩放实时重算列数,卡片拉伸填满行尾(小/中/大档位 180/210/240 联动,列宽公式按 ItemsWrapGrid 槽位语义实测校正)
- 新增:筛选结果增量同步——只删除/插入/移动真实变化的项,动画不再全列表重放;翻页保持无动画整页替换
- 新增:空状态提示(未扫描/无结果)淡入淡出动画(120ms),与列表动画节奏一致
- 移除:AnnotatedScrollBar(详情滚动条)——控件、ViewModel、设置项、多语言资源全部清除,滚动条回归系统默认,列表可用宽度增加
- 新增:壁纸长描述折叠(超出 5 行显示「展开全文/收起」)
- 新增:组件属性面板独立窗口(快照+多开,组件无 project.json 属性故不含「壁纸属性」页)
- 变更:图标档位 60→30 递增(180/210/240),壁纸页与组件页一致
- 新增:系统通知(AppNotification)
- 优化:文件属性页虚拟化行模板

## v0.5.0

- 新增:「导入解包」页——本地 .pkg/.mpkg 壁纸包拖拽/选择文件/扫描文件夹入队,重名自动加序号、按路径去重;「开始提取」走 repkg batch 管线,每项实时进度条(0.5% 阈值防抖 + 只前进不回退),状态流转(等待提取/正在提取/提取完成/失败/已停止),提取完成后补写缺失的 project.json 并触发后台扫描,壁纸直接出现在「我的壁纸」
- 新增:导出目录选择(Flyout:文本框 + 「项目目录」「导出目录」快捷填入 + 文件夹选择),500ms 防抖持久化到 config.json(Path.ImportExportPath),留空默认 Papers 页导出目录
- RePKG_Re 升级 v0.5.1:batch manifest 的 `input` 兼容**文件与目录**——指向单个 .pkg/.mpkg 文件时只拆该文件,导入页因此免去「先拷贝 pkg 到输出目录」的暂存步骤,输出目录为标准项目结构
- 新增:壁纸页搜索支持 WorkshopID(标题/ID 任一匹配,大小写不敏感)
- 新增:壁纸页搜索建议(AutoSuggestBox)——输入时在当前筛选结果(类型/分级/来源/标签限定)内匹配标题/ID,最多 8 条,显示「标题 (ID)」,选中回填完整标题保证可命中
- 修复:组件页每次切换后列表全量重建(观感像没有缓存)——LoadComponents 先 Clear 导致 ApplyFilters 的「结果未变化则跳过」优化失效,移除后内容未变时零重建
- 修复:导入页重名文件提取合并进同一目录(输出文件夹名与 Title 不一致导致)——Title 取实际文件夹名,内存+磁盘双检测防重名
- 属性面板:虚拟化属性列表改 code-behind 实例化(XamlCompiler 对窗口内 ItemsRepeater 标签稳定崩溃的规避);文本组件保留含 <img> 的行,支持链接/图片渲染

## v0.4.4

- 新增:壁纸属性面板——查看菜单/右键菜单可切换「属性面板」,读取壁纸目录 project.json 的 general.properties,按类型映射为可编辑控件(布尔开关/滑块/下拉/颜色/文本/场景纹理文件选择),保存时文本级定点写回 project.json;group 分组以 Expander 展示且默认折叠,大量分组的壁纸展开不卡
- 新增:属性面板整体滚动 + 渐进渲染——右侧面板改为整体滚动,属性分批加载(每批 10 项)并在加载期间显示 ProgressRing,切换大壁纸不冻结
- 修复:CheckBox 对齐(勾选框不靠右显示)
- 修复:Info 页 RePKG_Re 版本号 TrimEnd 实现把第三段为 0 的版本误剪(0.5.0 显示为 0.5)

## v0.4.3

- 新增:提取进度面板重构——多壁纸提取改为壁纸列表视图(预览图 + 名称 + 每壁纸实时进度条),替代原堆叠卡片;进度更新带 0.5% 阈值防抖 + 服务层 30ms 节流,批量提取时界面流畅不卡
- 新增:进度面板高度跟随窗口(上下边距自适应),壁纸列表随窗口伸缩,内容超高时滚动区滚动;总进度条与暂停/继续/停止按钮固定在面板底部,不随内容滚动
- 新增:Info 页 RePKG_Re 日志面板——独立记录 repkg 进程输出(常规输出行/错误/条目错误),每次提取开始清空,支持拖拽调高
- 修复:提取中点击停止偶发报错 "No process is associated with this object"(进程对象被停止流程与取消路径双重释放的竞态,统一由提取流程释放)
- 修复:筛选结果时 COMException 0x8000FFFF 崩溃(分页按钮绑定更新在后台线程执行,强制切回 UI 线程)
- 修复:中文文件名乱码(如"恶魔"显示为"榄旂帇")——repkg 输出统一 UTF-8,前端进程重定向显式按 UTF-8 解码
- 修复:属性面板文件夹树折叠再展开后子项翻倍(3→6)的问题(仅首次展开时加载子项)
- 修复:Info 页 RePKG_Re 版本号显示被误剪(0.5.0.0 显示为 0.5)
- 变更:进度面板标题固定为「正在提取」,移除左下角进度计数与两条分界线

## v0.4.2

- 新增:批量提取改为单进程 batch 模式(repkg_Re v0.5.0)——manifest 一次性下发、多线程并行提取、内存闸限制峰值内存、壁纸边界自动内存整理
- 新增:提取进程崩溃自动重启(最多 3 次),同一壁纸连续两次触发崩溃自动跳过并记录
- 新增:FakeRePkg 模拟器与崩溃重启测试(5 场景 25 断言)

## v0.4.1

- 新增:效果图剔除——设置页「自定义」输出模式下勾选后,转换图中透明或黑色像素占比 ≥ 阈值的图片整条目跳过(粒子/光效/黑底纹理等垃圾图不再混入输出);阈值 0-100% 可调,默认 85%(基于真实场景壁纸采样标定)
- 新增:目录过滤——「自定义」模式新增「仅解析目录/忽略解析目录」两组开关,支持多级子文件夹(如 materials/masks),正反斜杠均可
- 变更:输出类型「仅输出媒体文件」逻辑升级——只输出 materials/ 与 sounds/ 目录下的直接内容(排除 masks/effects/workshop 等子文件夹),并新增音频文件输出(mp3/ogg/wav/flac/m4a/aac)
- 变更:「剔除效果图」从输出设置迁入「自定义」模式,增加启用开关(勾选才生效)
- RePKG_Re 升级 v0.4.5:新增效果图剔除(--filter-effect-images)、目录前缀过滤(--onlypaths/--ignorepaths,含子文件夹)、深度限制(--paths-depth)
- 修复:壁纸/组件两页面 F5 刷新不生效(快捷键未接线,已补)
- 改进:组件页刷新与壁纸页同步——先清空显示列表再重填
- 改进:刷新按钮防连按——刷新期间按钮禁用,重复触发(F5/菜单/连点)自动忽略
- 改进:组件页右键菜单补齐快捷键提示(F5/Ctrl+A/Ctrl+I)
- 改进:「输出类型」帮助提示更新——媒体模式说明(含音频与目录限制)、自定义模式新组件说明,11 语言同步
- 修复:提取完成时进度回调越界崩溃(汇总消息不含分隔符的防御处理)
- 变更:设置页「启动页」选项增加图标,与导航栏图标一致
- 替换:无预览图占位图(noPreview.png)为代码生成的矢量风格文件图标
- 修正:Info 页头部布局调整与代码清理

## v0.4.0

- 新增：关于页重构——WE Tool / RePKG_Re 双 expander，各自带项目名标题、版权与版本号
- 新增：技术栈列表标注版本号（WE Tool 10 项 / RePKG_Re 6 项），发布前自动校验与 csproj 一致性
- 新增：贡献者卡片（头像 + 名称 + 主页链接），数据由 CSV 驱动、两个项目独立维护
- 新增：RePKG_Re 专区——项目由来、原项目仓库（notscuffed/repkg）链接，版本号随附带的 exe 动态读取
- 新增：许可证与第三方组件查看对话框（应用内全文 + 「在浏览器中查看」），两个 expander 均支持
- 新增：关于页底部引用说明与免责声明
- 变更：外部链接按钮改为纯 XAML 样式（悬停时文字淡出、图标淡入），移除代码版控件
- 修正：RePKG_Re 版权年份（2025-2026 → 2026，与仓库实际创建时间一致）
- 新增：实时日志面板（控制台风格黑底 + 按级别着色），日志统一为单文件 logs/log.txt
- 新增：Steamworks 状态 InfoBar（在线/初始化失败/断开三态 + 重试按钮），初始化异步化不再卡 UI
- 变更：Steamworks 改为独立桥接子进程注册（SteamworksBridge.exe，stdin/stdout 协议通信）——Steam 客户端退出时不再连带关闭应用，断线后可一键重连
- 新增：导航栏 Info 项状态徽标（Steamworks 与 RePKG_Re 全部正常才显示绿色，其余红色）
- 新增：RePKG_Re 后端版本校验 InfoBar（构建时从仓库 csproj 注入目标版本，运行时比对随包 exe）
- 新增：翻译进度卡片（11 语言完成度列表，以简体中文键集为基准，构建时自动统计）

## v0.2.2

- 新增：设置页底部"重置设置"按钮——确认后将所有设置恢复为默认值（UI 即时刷新，路径自动重新检测，语言/主题变化即时应用）
- 变更：输出类型"仅输出图像"升级为"仅输出媒体文件"——除图片外同时保留视频（pkg 内 .mp4/.webm/.mov 条目及视频纹理 TEX 转换出的 mp4），场景壁纸的视频不再丢失
- RePKG_Re 升级 v0.4.2：新增输出层扩展名过滤（-I / -E，即 --output-ignoreexts / --output-onlyexts），条目照常解析（TEX 照常转换），写文件时按输出文件扩展名判断，转换图按转换后格式参与过滤
- 变更：自定义模式的"忽略扩展名/仅提取扩展名"改用输出层过滤语义，场景壁纸 TEX 转换图按转换后格式（如 .png）参与判断，修复此前"仅保留 png"时场景壁纸无输出的问题
- 优化：lazy 模式跳过被过滤条目的字节读取
- 修复：输出类型枚举注释写反（实际为 0=全量输出、1=仅输出媒体文件、2=自定义）

## v0.2.1

- 新增：已安装组件页支持键盘快捷键（Ctrl+A 全选、Ctrl+I 反选、Ctrl+C 复制、Delete 删除、Alt+Enter 属性），与壁纸页行为一致
- 新增：未扫描到任何组件时的引导提示（含"前往设置"按钮），与壁纸页一致
- 新增：壁纸页与组件页属性面板文字支持框选复制
- 优化：组件页底部栏与壁纸页对齐（导出目录联动、自动检测、浏览/打开）
- 优化：文件结构树禁止拖拽节点（WinUI 默认样式允许拖拽，已显式关闭）
- 修复：Ctrl+I 反选快捷键不生效（KeyboardAccelerator 在部分焦点/输入法环境下不触发，改用 KeyDown 路由事件）

## v0.2.0

- 新增：组件页重构，壁纸/组件分页浏览与多选批量操作
- 新增：配置结构升级至 v2，旧配置自动迁移（筛选、排序、动图等设置不丢失）
- 新增：GitHub Actions 自动发布（tag 触发，生成自包含/框架依赖两种产物）
- 优化：界面文本全面 x:Uid 本地化（简体中文）
- 优化：动图开关变更时只刷新当前页面，不再清空全部页面缓存
- 优化：排序方向按钮改为图标表达状态
- 修复：发布产物缺失 repkg、重复发布时 launcher 移动失败
- 内嵌 repkg_Re 0.4.1（.NET Framework 4.7.2 单文件，免安装依赖）
- 协议：GPL-3.0

## v0.1.0

- 首个公开发布版本
