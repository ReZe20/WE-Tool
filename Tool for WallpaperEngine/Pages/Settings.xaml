<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Tool_for_WallpaperEngine.Settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tool_for_WallpaperEngine"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Required"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="设置" FontSize="25" VerticalAlignment="Center" Margin="30,0,0,0"/>
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel  Margin="25,10,25,0">
                    <TextBlock Text="启动时" Margin="5,10,0,10"/>
                    <Border Padding="12" CornerRadius="6" Margin="0,8,0,8" Background="{ThemeResource SystemControlBackgroundAltHighRevealBackgroundBrush}" BorderBrush="{ThemeResource SystemControlBackgroundBaseLowBrush}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <FontIcon Glyph="&#xE8A1;" FontSize="20" VerticalAlignment="Center" Margin="0,0,12,0"/>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="默认起始页" FontSize="15"/>
                                <TextBlock Text="选择启动 Tool for WallpaperEngine 时显示的页面" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                            </StackPanel>

                            <ComboBox Grid.Column="2" Width="160" VerticalAlignment="Center" HorizontalAlignment="Right" Padding="6,4" SelectedIndex="0">
                                <ComboBoxItem>
                                    <ComboBoxItem.Content>
                                        <StackPanel Orientation="Horizontal">
                                            <FontIcon Glyph="&#xE8B9;"/>
                                            <TextBlock Text="已安装壁纸" Margin="7,0,0,0"/>
                                        </StackPanel>
                                    </ComboBoxItem.Content>
                                </ComboBoxItem>
                                <ComboBoxItem>
                                    <ComboBoxItem.Content>
                                        <StackPanel Orientation="Horizontal">
                                            <FontIcon Glyph="&#xE8B5;" />
                                            <TextBlock Text="导入壁纸" Margin="7,0,0,0"/>
                                        </StackPanel>
                                    </ComboBoxItem.Content>
                                </ComboBoxItem>
                            </ComboBox>
                        </Grid>
                    </Border>

                    <TextBlock Text="提取时" Margin="5,10,0,10"/>
                    <StackPanel>
                        <controls:SettingsExpander Header="场景壁纸设置" Description="控制对 RePKG 的输入指令">
                            <controls:SettingsExpander.HeaderIcon>
                                <FontIcon Glyph="&#xE756;"/>
                            </controls:SettingsExpander.HeaderIcon>
                            <controls:SettingsExpander.Items>
                                <Grid Margin="65,10,0,10">
                                    <CheckBox Content="忽略扩展名" x:Name="IgnoreExtension" ToolTipService.ToolTip="不提取具有指定扩展名的文件"/>
                                    <TextBox HorizontalAlignment="Stretch" IsEnabled="{Binding ElementName=IgnoreExtension, Path=IsChecked}" Margin="125,0,20,0" ToolTipService.ToolTip="请使用英文逗号 &quot;,&quot; 做分隔符"/>
                                </Grid>

                                <Border BorderBrush="#E0E0E0" VerticalAlignment="Stretch" BorderThickness="1" Background="{ThemeResource SystemControlForegroundBaseLowBrush}"/>
                                <Grid Margin="65,10,0,10">
                                    <CheckBox Content="仅提取扩展名"  x:Name="OnlyExtension" ToolTipService.ToolTip="仅提取具有指定扩展名的文件"/>
                                    <TextBox HorizontalAlignment="Stretch" IsEnabled="{Binding ElementName=OnlyExtension, Path=IsChecked}" Margin="125,0,20,0" ToolTipService.ToolTip="请使用英文逗号 &quot;,&quot; 做分隔符"/>
                                </Grid>

                                <Border BorderBrush="#E0E0E0" VerticalAlignment="Stretch" BorderThickness="1" Background="{ThemeResource SystemControlForegroundBaseLowBrush}"/>
                                <CheckBox Content="将输入目录中所有 TEX 文件转换为图像" Margin="65,10,0,10"/>

                                <Border BorderBrush="#E0E0E0" VerticalAlignment="Stretch" BorderThickness="1" Background="{ThemeResource SystemControlForegroundBaseLowBrush}"/>
                                <CheckBox Content="将所有提取的文件应放入一个目录中，不用子文件夹分别存储" Margin="65,10,0,10"/>

                                <Border BorderBrush="#E0E0E0" VerticalAlignment="Stretch" BorderThickness="1" Background="{ThemeResource SystemControlForegroundBaseLowBrush}"/>
                                <CheckBox Content="同时将project.json与预览图导出至输出文件夹内" Margin="65,10,0,10"/>

                                <Border BorderBrush="#E0E0E0" VerticalAlignment="Stretch" BorderThickness="1" Background="{ThemeResource SystemControlForegroundBaseLowBrush}"/>
                                <CheckBox Content="使用项目project.json中的名称作为项目子目录的名称，而非 ID" Margin="65,10,0,10"/>

                                <Border BorderBrush="#E0E0E0" VerticalAlignment="Stretch" BorderThickness="1" Background="{ThemeResource SystemControlForegroundBaseLowBrush}"/>
                                <CheckBox Content="在提取 PKG 时不将 TEX 文件转换为图像" Margin="65,10,0,10"/>

                                <Border BorderBrush="#E0E0E0" VerticalAlignment="Stretch" BorderThickness="1" Background="{ThemeResource SystemControlForegroundBaseLowBrush}"/>
                                <CheckBox Content="覆盖所有已存在的文件" Margin="65,10,0,10"/>
                            </controls:SettingsExpander.Items>
                        </controls:SettingsExpander>
                    </StackPanel>
                    <Expander IsExpanded="True" HorizontalAlignment="Stretch" Background="{ThemeResource SystemControlBackgroundAltHighBrush}">
                        <Expander.Header>
                            <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                                <FontIcon Glyph="&#xE712;" FontSize="20"/>
                                <StackPanel>
                                    <TextBlock Text="场景壁纸提取设置"/>
                                    <TextBlock Text="控制对 RePKG 的输入指令" FontSize="12"/>
                                </StackPanel>
                            </StackPanel>
                        </Expander.Header>

                        <StackPanel>
                            <CheckBox Content="置于顶层" Padding="12,8" HorizontalAlignment="Left"/>
                            <CheckBox Content="使用时最小化" Padding="12,8"/>
                            <CheckBox Content="最小化时隐藏" Padding="12,8"/>
                        </StackPanel>
                    </Expander>
                    <Expander IsExpanded="True" HorizontalAlignment="Stretch" Background="{ThemeResource SystemControlBackgroundAltHighBrush}">
                        <Expander.Header>
                            <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                                <FontIcon Glyph="&#xE712;" FontSize="20"/>
                                <StackPanel>
                                    <TextBlock Text="窗口管理" FontWeight="SemiBold"/>
                                    <TextBlock Text="指定默认任务管理器窗口行为" FontSize="12"/>
                                </StackPanel>
                            </StackPanel>
                        </Expander.Header>

                        <StackPanel Margin="0,8,0,0">
                            <CheckBox Content="置于顶层" Padding="12,8"/>
                            <Border Height="1" Margin="0,6"/>
                            <CheckBox Content="使用时最小化" Padding="12,8"/>
                            <Border Height="1" Margin="0,6"/>
                            <CheckBox Content="最小化时隐藏" Padding="12,8"/>
                        </StackPanel>
                    </Expander>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
